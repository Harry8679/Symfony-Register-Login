{% extends 'base.html.twig' %}

{% block title %}Espace Inscription
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="text-center">Espace inscription</h1>
		<hr>
		<div class="row justify-content-center">
			<div
				class="col-md-6">
				{# {{ form(formRegister) }} #}
				{{ form_start(formRegister, {'attr': {'data-turbo': 'false'}}) }}
				{{ form_widget(formRegister) }}
				{{ form_end(formRegister) }}
			</div>
		</div>
	</div>
{% endblock %}
